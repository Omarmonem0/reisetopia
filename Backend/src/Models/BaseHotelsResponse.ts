export interface BaseHotelsResponse {
    id: number,
    minPrice: number,
    currencyCode: string,
    countryCode: string,
    name: string,
    address: string,
    city: string,
    description: string,
    distanceToCenterKm: number,
}